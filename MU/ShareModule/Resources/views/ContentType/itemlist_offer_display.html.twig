{# Purpose of this template: Display offers within an external context #}
{% for offer in items %}
    <h3>{{ offer|musharemodule_formattedTitle }}</h3>
    <p><a href="{{ path('musharemodule_offer_display', {'id': offer.getId()}) }}" title="{{ __('Read more')|e('html_attr') }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
