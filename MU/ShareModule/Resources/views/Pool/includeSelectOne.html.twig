{# purpose of this template: inclusion template for managing related pool #}
{% if displayMode is not defined or displayMode is empty %}
    {% set displayMode = 'choices' %}
{% endif %}
{% if tabs|default(false) == true %}
    <div role="tabpanel" class="tab-pane fade" id="tabPool" aria-labelledby="poolTab">
        <h3>{{ heading|default ? heading : __('Pool') }}</h3>
{% else %}
    <fieldset class="pool">
{% endif %}
    <legend>{{ heading|default ? heading : __('Pool') }}</legend>
    {% if displayMode == 'choices' %}
        {{ form_row(attribute(form, alias)) }}
    {% elseif displayMode == 'autocomplete' %}
        {{ form_row(attribute(form, alias)) }}
        <div class="musharemodule-relation-leftside">
            {{ include(
                '@MUShareModule/Pool/Admin/includeSelectItemListOne.html.twig',
                attribute(linkingItem, alias) is defined ? {item: attribute(linkingItem, alias)} : {}
            ) }}
        </div>
        <br style="clear: both" />
    {% endif %}
{% if tabs|default(false) == true %}
    </div>
{% else %}
    </fieldset>
{% endif %}
