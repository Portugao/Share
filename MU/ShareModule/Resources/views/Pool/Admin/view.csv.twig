{# purpose of this template: pools view csv view #}
{% spaceless %}"{{ __('Collection of pool') }}";"{{ __('Workflow state') }}"
;"{{ __('Offers') }}"
{% endspaceless %}
{% for pool in items %}
{% spaceless %}
    "{{ pool.collectionOfPool }}";"{{ pool.workflowState|musharemodule_objectState(false)|lower }}"
    ;"
    {% if pool.offers|default %}
        {% for relatedItem in pool.offers %}
        {{ relatedItem|musharemodule_formattedTitle }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    {% endif %}
    "
{% endspaceless %}
{% endfor %}
